{
  "name": "cf-mony-worker",
  "version": "1.0.0",
  "private": true,
  "main": "index.js",
  "dependencies": {
    "@cloudflare/kv-asset-handler": "^0.4.2",
    "itty-router": "^4.0.23",
    "selenium-webdriver": "^4.35.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.2.4",
    "esbuild": "^0.28.0",
    "jsdom": "^26.1.0",
    "miniflare": "^4.20260603.0",
    "vitest": "^3.2.4"
  },
  "scripts": {
    "test": "vitest run --reporter=verbose",
    "test:unit": "vitest run --reporter=verbose index.test.js static-assets.test.js",
    "test:integration": "node scripts/build-test-worker.mjs && vitest run --reporter=verbose integration.test.js",
    "test:all": "vitest run --reporter=verbose",
    "test:coverage": "vitest --coverage"
  }
}
